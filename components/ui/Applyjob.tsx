import React from 'react'
import MagicButton from './MagicButton'

export default function Applyjob({}) {
  return (
    <div>
        <MagicButton title="Apply Now" icon="" position="" handleClick={()=> JobApplicationSubmission(currentId)} />
    </div>
  )
}
